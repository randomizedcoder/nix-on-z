diff --git a/src/libmain/unix/stack.cc b/src/libmain/unix/stack.cc
index bec0d389f..6080c6f68 100644
--- a/src/libmain/unix/stack.cc
+++ b/src/libmain/unix/stack.cc
@@ -21,6 +21,8 @@ static void sigsegvHandler(int signo, siginfo_t * info, void * ctx)
     sp = (char *) ((ucontext_t *) ctx)->uc_mcontext.gregs[REG_RSP];
 #elif defined(REG_ESP)
     sp = (char *) ((ucontext_t *) ctx)->uc_mcontext.gregs[REG_ESP];
+#elif defined(__s390x__)
+    sp = (char *) ((ucontext_t *) ctx)->uc_mcontext.gregs[15]; /* R15 = stack pointer on s390x */
 #else
     haveSP = false;
 #endif
diff --git a/src/libstore/unix/build/linux-derivation-builder.cc b/src/libstore/unix/build/linux-derivation-builder.cc
index ca0a26ab5..497d80b19 100644
--- a/src/libstore/unix/build/linux-derivation-builder.cc
+++ b/src/libstore/unix/build/linux-derivation-builder.cc
@@ -65,6 +65,9 @@ static void setupSeccomp(const LocalSettings & localSettings)
     if (nativeSystem == "mips64el-linux" && seccomp_arch_add(ctx, SCMP_ARCH_MIPSEL64N32) != 0)
         printError("unable to add mips64el-*abin32 seccomp architecture");
 
+    if (nativeSystem == "s390x-linux" && seccomp_arch_add(ctx, SCMP_ARCH_S390) != 0)
+        printError("unable to add s390 seccomp architecture");
+
     /* Prevent builders from creating setuid/setgid binaries. */
     for (int perm : {S_ISUID, S_ISGID}) {
         if (seccomp_rule_add(
